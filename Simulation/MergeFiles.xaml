<UserControl x:Class="Simulation.MergeFiles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:converters="clr-namespace:Simulation.Converters" >
    <!--  Merge files and reorganize the simulation data -->
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  Flip view header  -->
        <Label Grid.Row="0">Step 2: View results and export</Label>

        <!--  Flip view description  -->
        <Label Grid.Row="1" FontSize="11">
            <TextBlock TextWrapping="WrapWithOverflow">
                Below is the merged simulation data. Reorder columns if needed. Export if the result looks good.
            </TextBlock>
        </Label>

        <!--  FILES/DATA tabs  -->
        <Grid Grid.Row="2">
            <DataGrid CanUserSortColumns="True" CanUserReorderColumns="False" Name="grdFileDataGrid" ItemsSource="{Binding Path=DefaultView, Mode=OneWay}"></DataGrid>
        </Grid>

    </Grid>
</UserControl>
